# 雀魂 牌譜URL → JSON エクスポータ

雀魂の牌譜URLを貼り付けると、AIが読める JSON になる。それだけの変換器。

**状態：実装保留（ren の L3 判断待ち）**

- 仕様・受け入れ条件・規約リスク → `SPEC.md`
- 調査結果 → `RESEARCH.md`（2026-07-26・ニア）

## いまの状況

技術的には作れる。ただし成立させるには **ren の雀魂アカウントの access_token で非公式APIを叩く**ことが
事実上必須で、それが利用規約 第11条（不正な方法でのコンテンツ取得／リバースエンジニアリング）に当たる読みができる。
**BAN されるのは ren のアカウント**なので、AI が引き受けられる判断ではない（＝L3）。

## 次の一手（ren にお願いしたい30秒）

**`https://mjai.ekyu.moe/` をブラウザで開いて、雀魂の牌譜URLをそのまま受け付けるか／
tenhou.net/6 形式の JSON を落とせるか**を見てほしい。

- **落とせる** → 作るのは iOSショートカット1本で済む。token も規約リスクも自前で背負わない（`SPEC.md` §7 案A）
- **落とせない** → `SPEC.md` §7 の案B/C/D から選ぶ判断になる

クラウド環境からは egress ポリシーで 403 になり、確認できなかった。

## 動かし方

（実装後に書く）
