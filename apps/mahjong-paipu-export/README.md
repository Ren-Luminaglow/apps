# 雀魂 牌譜URL → JSON エクスポータ

雀魂の牌譜URLを貼り付けると、AIが読める JSON になる。それだけの変換器。

**状態：実装保留（ren の L3 判断待ち）**

- 仕様・受け入れ条件・規約リスク → `SPEC.md`
- 調査結果 → `RESEARCH.md`（2026-07-26・ニア）

## いまの状況

**対象は三麻**（2026-07-27 判明）。これが効いて、状況が2つの壁になった。

1. **技術の壁（新）**：変換系OSSはほぼ全部が「4人麻雀のみ」。三麻の雀魂レコードを
   正しく JSON にする実装が世に無い。既存サービスに投げる案（`mjai.ekyu.moe`）も三麻非対応で消滅。
2. **規約の壁**：牌譜取得には ren のアカウントの access_token が要り、それが
   利用規約 第11条に当たる読みができる。**BAN されるのは ren のアカウント**＝L3。

## 次の一手

**先に 1（技術の壁）を潰す。** 規約リスクを飲む判断をしても、三麻を変換できなければ意味がないため。
調べること → `SPEC.md` §5-1。

## 動かし方

（実装後に書く）
