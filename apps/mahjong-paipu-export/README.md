# 雀魂 牌譜URL → JSON エクスポータ

雀魂の牌譜URLを貼り付けると、AIが読める JSON になる。それだけの変換器。

**状態：設計中（未実装）**

- 仕様・受け入れ条件 → `SPEC.md`
- 調査結果 → `RESEARCH.md`（未着手）

## 次の一手

`SPEC.md` §5 の調査を閉じる（→ ニア）。
特に「牌譜URLから対局データを取れるのか／認証は要るのか」が決まらないと形が決まらない。

## 動かし方

（実装後に書く）
