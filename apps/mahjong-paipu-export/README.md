# 雀魂 牌譜URL → JSON エクスポータ

雀魂の牌譜URLを貼り付けると、AIが読める JSON になる。それだけの変換器。

**状態：実装保留（ren の L3 判断待ち）**

- 仕様・受け入れ条件・規約リスク → `SPEC.md`
- 調査結果 → `RESEARCH.md`（2026-07-26・ニア）

## いまの状況

**対象は三麻。技術的な壁は無くなった。残るブロッカーは規約判断ひとつ。**

- **技術：クリア。** 雀魂の protobuf は三麻でも四麻と同じ構造で、北抜きだけが専用レコード（`RecordBaBei`）。
  出力形式（tenhou.net/6 / mjai）も両方とも三麻を表現できる。
  `tensoul`（MIT）には三麻分岐が最初から入っていた（README に書かれていないだけ）。
  → 自作の規模は **600〜1000行、1ファイル相当**（`SPEC.md` §5.5）。
- **規約：未決＝L3。** 牌譜取得には ren のアカウントの access_token が要り、
  それが利用規約 第11条に当たる読みができる。**BAN されるのは ren のアカウント**なので AI は決められない。

## 次の一手

**`SPEC.md` §7 の B / C / D を ren が選ぶ。** それが決まれば実装に入れる。

- **B**：token を Worker に置いて自前取得（iPhone完結・規約リスク高）
- **C**：PCブラウザ拡張で WebSocket 傍受（認証情報を保存しない・**iPhone完結は諦める**）
- **D**：やらない（調査結果は資産として残す）

## 動かし方

（実装後に書く）

## 動かし方

（実装後に書く）
