# CHANGELOG — 雀魂 牌譜URL → JSON エクスポータ

## [Unreleased]

### 2026-07-27（その2・三麻調査の結果）
- `RESEARCH_sanma.md` を追加（ニア・一次ソース＝protobuf定義と実コードを直接読んだ調査）。
- **「変換系OSSは三麻ほぼ全滅」を撤回・訂正。** README だけで判断していたのが誤りだった。
  実際には `Equim-chan/tensoul`（MIT）の `convert.js` に三麻分岐が最初から実装されている
  （`if (3 == nplayers)` / `case "RecordBaBei"` → `"f44"` / `TSUMOLOSSOFF`）。
- 雀魂の protobuf は三麻でも四麻と同じ構造（`tiles3` が空・`scores` が3要素・萬子2-8が出ないだけ）。
  北抜きのみ専用レコード `RecordBaBei{seat, doras, operations, moqie}`。
- 出力形式は tenhou.net/6 / mjai の両方が三麻を表現可能。北抜きは `"f44"` / `Nukidora{actor,pai}` で
  独立実装間の表記が一致。三麻を扱う実装は少なくとも4つ実在。
- **未確定「三麻の変換」は解消。** ボトルネックではなくなった。
- §5.5（実装方針）を新設。**案③＝protobuf から直接自作（600〜1000行）を採用**、
  tensoul を答え合わせ用の参照実装にする。ライセンス境界（MahjongCopilot は GPLv3 なので読まない）も明記。
- §7 を更新：**残るブロッカーは規約判断（B/C/D）の一点のみ。**

### 2026-07-27（その1）
- **対象が三人麻雀（三麻）であることが判明**。SPEC §1 に最上位の制約として明記。
  v0.1 の SPEC がこの前提を欠いていたため、調査の結論の一部が無効になった。
- **案A（既存サービスに変換させる）が消滅**。`mjai.ekyu.moe` が三麻に非対応（ren 確認）。
- 調査資料を三麻の観点で読み直し：変換系OSSはほぼ全て「4人麻雀のみ」または
  「三麻は想定外挙動」と明記しており、**流用できる変換器がほぼ無い**ことが判明（SPEC §5 に表を追加）。
- 未確定の最重要項目を「三麻の変換をどうするか」に差し替え。
  規約判断（L3）より先にこちらを潰す方針に変更（変換できなければ判断の意味がないため）。

### 2026-07-26
- ニアの調査完了 → `RESEARCH.md` を追加。
- SPEC を v0.2 に更新：
  - **状態を「実装保留（L3判断待ち）」に変更。** 規約リスクが ren 判断事項として確定したため。
  - §3 入口を「共有シート」→「**クリップボード**」に修正（雀魂アプリに共有シート導線が無いため）。
  - §5 を調査結果で置き換え。残った未確定は3点（mjai.ekyu.moe の可否／token有効期限／出力サイズ）。
  - §6 に「バルク収集をしない」を追加。
  - **§7（規約リスク・L3）を新設。** 案A〜Dの選択肢を整理。
- 判明した主な事実：牌譜URLのGETでは中身が取れず `fetchGameRecord`（WS+protobuf）＋認証が必須／
  JPはYostar ID移行で既存OSSのログイン実装が使えない／出力はmjai形式がAI向けに有利。

### 2026-07-26（初回）
- SPEC v0.1 draft を作成。スコープ（URL → JSON の変換器のみ）と受け入れ条件を確定。
- 未確定として、牌譜データの取得経路・出力フォーマット・iPhoneからの入口・利用規約の4点を洗い出し。
